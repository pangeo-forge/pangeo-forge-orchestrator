
{
  "name": "Pangeo Forge API",
  "description": "FastAPI / Postgres Backend App",
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "pip install -e '.[dev]'",
        "test": "pytest -v"
      }
    }
  }
}
