{
  "name": "Pangeo Forge API",
  "description": "FastAPI / Postgres Backend App",
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "git tag; git status; git describe; pip install -e '.[dev]'",
        "test": "pytest -v tests"
      }
    }
  }
}
