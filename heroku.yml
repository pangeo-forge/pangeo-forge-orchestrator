setup:
  addons:
    - plan: heroku-postgresql:standard-0
    - plan: papertrail:fixa
build:
  docker:
    web: Dockerfile
release:
  command:
    - ./scripts.deploy/release.sh
  image: web
run:
  web: ./scripts.deploy/run.sh
