setup:
  addons:
    - plan: heroku-postgresql:standard-0
    - plan: papertrail:fixa
build:
  docker:
    web: Dockerfile
release:
  command:
    - ./scripts.deploy/release.sh
  image: web
run:
  command:
    - ./scripts.deploy/run.sh
  image: web
